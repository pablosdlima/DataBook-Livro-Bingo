@model IEnumerable<DataBook_Bingo.Models.Shinobi>


<br />
<h3 style="text-indent:25px; font-family:Calibri;">Shinobis já Cadastradas...</h3>
<h5><a asp-action="Create">Inserir Novo</a></h5>
<br />

<div style="padding-left:8%;" class="row">
    @foreach (var item in Model)
    {
        <figure class="col-md-2 border-left border-bottom">
            @if (item.ImagemShinobi != null)
            {
                <figcaption align="center">@Html.DisplayFor(modelItem => item.NomeShinobi)</figcaption>
                <hr />
                var _img64 = Convert.ToBase64String(item.ImagemShinobi);
                var imgScr = String.Format("data:image/gif;base64,{0}", _img64);
                <img src="@imgScr" alt="Teste" style="margin-left:5%; width:90%;" class="figure-img" />
            }
        </figure>
    }
    <figure class="col-md-2 border-left">
    </figure>
</div>